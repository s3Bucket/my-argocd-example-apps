apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cschmidt_intro.fullname" . }}-test-connection"
  labels:
    {{- include "cschmidt_intro.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cschmidt_intro.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
